{
  "description": "Load complete Project Direction, canonical source identity and private preferences at supported task lifecycle boundaries.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "^(startup|resume|clear|compact)$",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/project-direction\" hook",
            "statusMessage": "Loading Project Direction",
            "additionalContextLimit": 16000
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/project-direction\" hook",
            "statusMessage": "Checking Project Direction",
            "additionalContextLimit": 16000
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/project-direction\" hook",
            "statusMessage": "Loading Project Direction for subagent",
            "additionalContextLimit": 16000
          }
        ]
      }
    ]
  }
}
