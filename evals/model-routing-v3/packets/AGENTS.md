# Isolated Benchmark Fixture

Read `VISION.md`, `MISSION.md`, `OBJECTIVE.md`, and `TASK.md` in full before
working. `TASK.md` is the complete implementation contract.

The external controller selected one implementation owner. Do not choose,
change, or add a model route. Do not delegate, spawn agents, consult another
model, or use external tools or services.

Apply the Algorithm to the task: confirm the useful outcome and proof, delete
unnecessary work, simplify the surviving change, use the shortest safe
feedback loop, and automate only a stable recurring loop. Keep every step
inside this exported workspace.

The implementation owner is the sole writer. Edit only paths allowed by
`TASK.md`. Do not inspect parent or sibling directories, Codex state, memories,
credentials, external repositories, Git remotes, or hidden verifier state. Do
not install dependencies, use the network, stage, commit, or mutate external
state.

Make the smallest complete change and run the public verifier from `TASK.md`.
The external controller owns exact scope checks, hidden verification, timing,
telemetry, and read-only review. Finish with the required structured result.
